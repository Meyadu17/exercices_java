package fr.apside.exerciceJava.exerciceBonus.factory_avec_api;
/*
import lombok.AllArgsConstructor;
import lombok.Getter;
import org.apache.commons.lang3.StringUtils;

import java.util.EnumSet;

@AllArgsConstructor*/
public enum EtapeReponsesEnum {
	/*REPONSES_PAGE_EN_COURS("encours"),
	REPONSES("reponses");

	@Getter
	private final String code;

	public EtapeReponsesEnum getEnum(String code) {
		return EnumSet.allOf(EtapeReponsesEnum.class)
				.stream()
				.filter(
						codeEtapeEnum -> StringUtils.equals(codeEtapeEnum.getCode(), code))
				.findAny().orElse(REPONSES);
	}*/
}
