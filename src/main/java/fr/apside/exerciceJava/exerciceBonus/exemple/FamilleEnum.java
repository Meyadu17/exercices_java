package fr.apside.exerciceJava.exerciceBonus.exemple;

//import lombok.AllArgsConstructor;
//import lombok.Getter;
//import org.apache.commons.lang3.StringUtils;

import java.util.EnumSet;

//@AllArgsConstructor
public enum FamilleEnum {
/*
	TOTO("codeToto"),
	TATA("codeTata");

	@Getter
	String code;

	public FamilleEnum getEnum(String code) {
		return EnumSet.allOf(FamilleEnum.class).stream().filter(
				familleEnum -> StringUtils.equals(familleEnum.getCode(), code)).findAny().orElse(TOTO);

	}
 */
}
